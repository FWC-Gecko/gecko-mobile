import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 5,
    borderRadius: 5,
  },
  upBg: {backgroundColor: '#14C881'},
  downBg: {backgroundColor: '#DF2040'},
  value: {color: 'white', marginHorizontal: 3},
});

export default styles;
